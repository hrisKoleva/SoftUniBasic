using System;

namespace ThirdLatinLetters
{
    class LatinLetters
    {
        static void Main(string[] args)
        {
            char latinLetter = 'a';
            while(latinLetter <= 'z')
            {
                Console.WriteLine(latinLetter);
                latinLetter++;

            }

            //for (char i = 'a'; i <= 'z'; i++)
            //{
            //    Console.WriteLine(i);
            //}
        }
    }
}
